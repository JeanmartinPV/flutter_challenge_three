name: flutter_challenge_three
description: A new flutter module project.
version: 1.0.0+1

environment:
  sdk: ">=2.16.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0
  http: ^0.13.4


flutter:

  uses-material-design: true

  assets:
    - assets/

  module:
    androidX: true
    androidPackage: com.example.flutter_challenge_three
    iosBundleIdentifier: com.example.flutterChallengeThree
